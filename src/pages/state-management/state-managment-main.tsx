import type React from "react";
import { Outlet } from "react-router-dom";

const StateManagementMain: React.FC<{}> = () => {
  return (
    <main>
      <header>Welcome To React-Readux Main Page</header>
    </main>
  );
};

export default StateManagementMain;
